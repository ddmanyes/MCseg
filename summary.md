# Pipeline Summary

- [2026-03-13] 完成專案深度審查與 QC 邏輯升級。
- [2026-03-13] 完成 MAD 公式修正與 Complexity Score 部署。
- [2026-03-14] 修正 Xenium 匯出的 transcripts 資料來源：由 Xenium instrument transcripts.parquet（480 genes）改為 Visium HD 2µm bin AnnData（14,597 genes），輸出 837,530 筆轉錄點，均勻覆蓋組織 ROI。
- [2026-03-19] 新增 Tissue Profile 系統，支援多組織分析（換組織只需修改 pipeline.yaml 一行）。
  - 建立 `config/profiles/crc.yaml`：CRC 組織基底（cyto2, dia 30/60 µm, 8 TME panels, 7 marker gene 群組）
  - 建立 `config/profiles/luad.yaml`：LUAD 組織基底（nuclei, dia 8/20 µm, 4 TME panels，xenium_he_seg 2026-03 驗證）
  - 合併順序：profile.yaml（基底）← pipeline.yaml（專案覆寫）← state.json（執行期覆寫）
- [2026-03-19] RNA 計數品質優化（xenium_he_seg benchmark 結果落地）。
  - `counter.py` 新增 `expand_labels` dilation（預設 6px = 1.64 µm）
  - Bins 指派率 +5–8 pp，PQ 0.397 → 0.432（+9%），改善 bins-in-gaps 問題
  - `rna_counting.dilation_px` 從 `pipeline.yaml` / profile 讀取
- [2026-03-19] TME Panel 動態化：`analysis/pipeline.py` 新增 `_build_tme_config(config)`，TME panels 與 immune_panel_map 改由 tissue profile YAML 驅動，不再硬編碼。
- [2026-03-19] Stage 2.5 Proseg RNA 升級至 Method E（VHD Proseg 精修 + Hungarian 回填）。
  - `runner.py` 新增 `_combine_refine_rna()`：Proseg centroid → 稀疏 Cellpose lookup + fallback 直接 bin 指派（全向量化）
  - `pipeline.yaml` stage25 更新最佳參數：max_dist=10µm, compactness=0.04, burnin=500, `hungarian_refine: true`
  - 輸出 `proseg_cells.h5ad` 細胞數固定 = Cellpose 細胞數，obs 含 `refined` boolean 欄位（n_refined / n_fallback 記錄於 uns）
  - 修復 4 個 code review 問題：BUG1（center_of_mass single-element crash）、BUG2（n_cp=0 早退）、BUG3（`__import__` 風格）、BUG5（gene mismatch silent discard → 加 warning）
- [2026-03-31] **Stage 3.5 空間基因表達探索器**上線。
  - 新增 `/spatial` 頁面（Stage35_SpatialExplorer），位於 Analysis 與 Export 之間
  - 支援 Contour 模式（細胞填色 + 深色 inner border 分隔）與 Gene Set 模式（多基因平均分 → 輪廓著色）
  - 8 組預設基因集（CD4 T / CD8 T / NK / Macrophage / B cell / Tumor / Fibroblast / Endothelial）；preset 採 case-insensitive 匹配，支援小鼠 Title Case 基因名
  - Merge ROI 模式自動依 `obs["roi"]` 分組，各 ROI 獨立渲染成 grid（上限 12 格）
  - 300 DPI PNG 下載，檔名自動帶 ROI / 模式 / 基因集名
  - `_parse_cell_id` 嚴格 regex 解析，非 `cell_N` format（如 barcode）提前報錯，避免靜默資料污染
- [2026-03-31] **MSseg v1.0 建立**（基於 visiumHD_pipeline_3，整合 MCseg v2 分割引擎）。
  - 核心分割由 LOGIC_A 雙尺寸策略改為 **MCseg v2 多模型 Voronoi 集成**（cyto3 三直徑 13/17/22px + Hematoxylin pass + Voronoi 擴張）
  - LUAD PQ@0.5：0.432（cellpose_dilate）→ **0.554**（MCseg v2，+28%）
  - 移除 Stage 2.5 Proseg（backend/src/proseg/、api/proseg_rna.py、前端 Stage25 頁面）
  - 新增 per-ROI 參數覆寫（dia_mid、voronoi_distance 等），轉錄本密度補救（選配）
  - pyproject.toml：name=msseg, version=1.0.0；前端品牌更新為 MSseg / MCseg v2
